#!/bin/sh

usage()
{
  cat <<EOF
Usage: eclconfig [OPTIONS] [LIBS]
Options: 
	[--cflags]
	[--libs|--ldflags]
Libs:
	cmp
EOF
}

LDFLAGS="-lecl -lclos"
for i in $*; do
  case $i in
    --cflags|-c)
      echo_cflags=yes
      ;;
    --libs|--ldflags|-l)
      echo_ldflags=yes
      ;;
    cmp)
      LDFLAGS="$LDFLAGS -lcmp"
      ;;
    *)
      usage 1 >&2
      ;;
    esac;
done

if test "$echo_cflags" = "yes"; then
  echo "@CFLAGS@ -D@host@ -I@includedir@"
fi

if test "$echo_ldflags" = "yes"; then
  echo "-L@libdir@ $LDFLAGS -llsp @LDFLAGS@ @CLIBS@"
fi
